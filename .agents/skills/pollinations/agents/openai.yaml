interface:
  display_name: "Pollinations"
  short_description: "Repo-specific patterns and workflows for pollinations"
  default_prompt: "Use the pollinations repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true